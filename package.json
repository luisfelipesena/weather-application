{
	"name": "weather-application",
	"version": "1.0.0",
	"scripts": {
		"install:all": "cd frontend && npm install && cd ../backend && npm install",
		"start:all": "concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\""
	},
	"author": "Luis Felipe",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.0.1"
	}
}
